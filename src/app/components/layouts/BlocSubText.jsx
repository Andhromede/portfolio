import { React } from "react";


const BlocSubText = ({ text }) => {

    return (
        <div className="col-span-12 text-gris-darkLight pl-2 text-justify text-[0.9em] italic">
            {text}
        </div>
    );
};

export default BlocSubText;
